# Discord
discord.py[voice]>=2.3.0
PyNaCl>=1.5.0

# STT
pywhispercpp>=1.2.0     # Whisper bindings — or build whisper.cpp manually

# TTS
kokoro-onnx>=0.4.0      # Kokoro TTS
soundfile>=0.12.1       # For writing WAV from Kokoro output
onnxruntime>=1.17.0     # ONNX runtime for Kokoro

# Utilities
python-dotenv>=1.0.0

# Optional — only needed for Piper (CLI-based, no pip install)
# Download piper binary from: https://github.com/rhasspy/piper/releases
